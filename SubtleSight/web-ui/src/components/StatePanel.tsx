export function StatePanel({loading,error,empty,onRetry,children}:{loading?:boolean;error?:unknown;empty?:boolean;onRetry?:()=>void;children:React.ReactNode}){if(loading)return <div className="state-panel"><span className="native-spinner"/><strong>正在整理情报…</strong></div>;if(error)return <div className="state-panel"><span className="state-glyph">!</span><h2>数据暂时不可用</h2><p>{(error as Error).message}</p>{onRetry&&<button className="native-button" onClick={onRetry}>重试</button>}</div>;if(empty)return <div className="state-panel"><span className="state-glyph">微</span><h2>还没有情报</h2><p>添加来源或发起一次全网发现，结果会出现在这里。</p></div>;return <>{children}</>}
